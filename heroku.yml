setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.cpython
#   config:
#     RAILS_ENV: development
#     FOO: bar
# release:
#   command:
#     - ./deployment-tasks.sh
#   image: web
